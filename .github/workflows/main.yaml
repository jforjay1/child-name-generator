name: Example Workflow with OpenAI API

on:
  push:
    branches:
      - main

jobs:
  example-job:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    
    - name: Example using OpenAI API
      env:
        OPENAI_SECRET_KEY: ${{ secrets.OPENAI_SECRET_KEY }}
      run: |
        streamlit run app.py
